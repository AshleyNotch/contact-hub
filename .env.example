# Copy to .env for local dev with `bun dev` / `npm run dev`
# Copy to .dev.vars for local dev with `wrangler pages dev`
# Set both vars in the Cloudflare Pages dashboard for production.

VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here
